/**
 * Study Plan Merge Logic
 *
 * @fileType stub
 * @domain learning
 * @ai-summary Merge logic was intentionally removed — on regeneration, completion and override data are cleared per user requirement.
 */

// Merge logic removed: regeneration now resets all days to planned (fresh recommendation).
// See: user requirement — on regeneration, completion and overrides are cleared.
export {}
