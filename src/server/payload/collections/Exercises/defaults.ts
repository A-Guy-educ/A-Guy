/**
 * Re-export defaults from shared module
 * This maintains backward compatibility while using shared types
 */
export {
  DEFAULT_CONTENT,
  ExerciseBlockDefaults,
  generateId,
} from '@/infra/llm/services/exercise-content/defaults'
