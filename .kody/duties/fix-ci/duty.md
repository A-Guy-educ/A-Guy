# Fix CI

## Purpose

Public `@kody fix-ci` action for repairing failing CI on an existing pull request.

## Instructions

Use the `fix-ci` executable for the CI repair workflow.
The duty owns the public action contract; the executable owns the method.
