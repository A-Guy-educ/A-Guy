# Classify

## Purpose

Public `@kody classify` action for routing an issue to the right work type.

## Instructions

Use the `classify` executable for classification and handoff.
The duty owns the public action contract; the executable owns the method.
