Resolved merge conflict in `.github/workflows/ci.yml`. The conflict was symmetric — all 8 occurrences were identical in nature: HEAD had `version: 10.33.0` (unquoted) while origin/dev had `version: '10.33.0'` (single-quoted). Both are valid YAML; chose the quoted form from origin/dev as it is more explicit about string type and avoids any YAML parsing ambiguity. No other changes were made.
