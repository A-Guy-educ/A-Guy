Resolved the single merge conflict in `.kody/reports/duty-review.md` by taking the origin/dev side (Cycle 14). The PR is for a Gravatar CSP fix — the duty-review report is an unrelated auto-generated file from the kody duty system. Cycle 14 is the more recent run and contains updated state, so it takes precedence. No conflict markers remain.
