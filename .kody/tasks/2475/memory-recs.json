[
  {
    "type": "lesson",
    "name": "serverexternalpackages-vs-webpackconfig-externals",
    "hook": "serverExternalPackages prevents Next.js serverless bundling; webpackConfig.externals prevents webpack from analyzing a package's internals",
    "body": "serverExternalPackages tells the Next.js build system to leave a package out of the serverless function bundle (loaded from node_modules at runtime). But webpack still analyzes the package's source when bundling the app code that imports it — for tree-shaking, dead code elimination, etc. webpackConfig.externals tells webpack to treat the package as an external Node.js module and skip all source analysis. Both are needed: serverExternalPackages for the bundler output, webpackConfig.externals for preventing webpack from encountering node: imports during analysis.",
    "why": "node-fetch was in serverExternalPackages (correctly, to reduce bundle size) but NOT in webpackConfig.externals. Webpack still analyzed node-fetch's internals for tree-shaking, encountered node:https/node:http/node:net/node:dns imports, and threw UnhandledSchemeError.",
    "confidence": 0.95
  },
  {
    "type": "lesson",
    "name": "resolve-alias-cannot-intercept-node-protocol",
    "hook": "resolve.alias CANNOT intercept node: protocol imports — use resolve.fallback instead",
    "body": "webpack resolve.alias operates on module names after scheme parsing, but webpack 5's scheme handler (ResolvePluginFactory.createResolver with 'node' scheme) runs BEFORE alias resolution and throws UnhandledSchemeError for node:fs, node:os, etc. The correct fix is webpackConfig.resolve.fallback = { node: stubPath } — FallbackPlugin is checked during scheme resolution and returns the fallback value instead of throwing. Additionally, set webpackConfig.resolve.fallback.node = stubPath (not the individual node:fs aliases) — the 'node' key handles all node: protocol requests because the scheme handler delegates to ResolvePlugin for the 'node' request name.",
    "why": "The previous session's fix used resolve.alias entries for node:fs, node:os etc. which are NEVER consulted because the node: scheme handler throws before alias resolution runs. The FallbackPlugin is the correct hook.",
    "confidence": 0.95
  }
]
