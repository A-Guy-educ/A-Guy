Deleted the redundant `.github/workflows/preview-validation.yml` workflow file. The workflow was redundant with `preview-comment.yml` — both triggered on `deployment_status`, but `preview-validation.yml` only checked HTTP status codes (200 = pass), producing false-positive "all good" comments on blank/error pages. Vercel already signals deployment success/failure via `deployment_status.state`, so `preview-comment.yml` is sufficient. No application code was changed.
