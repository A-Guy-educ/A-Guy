Resolved one merge conflict in `.kody/reports/duty-review.md`. The conflict was asymmetric: the PR branch (HEAD) had an empty file before the conflict markers, while origin/dev had the full Cycle 16 duty review table. Since the PR branch did not intentionally modify this operational tooling file and origin/dev had substantive content, the origin/dev version was kept. No quality gates needed as the change is non-code.
