Resolved merge conflict in `.kody/reports/duty-review.md`. The file is a rolling status snapshot of the kody duty system. HEAD (PR branch) had Cycle 12 with many unknown cadences ("?") while origin/dev had Cycle 16 with filled-in cadence values (15m, 1h, 7d, 1d). Took origin/dev as the resolved content — it represents the more current operational state. No functional code conflicts in this merge.
