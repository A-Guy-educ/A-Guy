Resolved merge conflict in `.kody/reports/duty-review.md`. Symmetric conflict: both sides had the same table structure with different cycle data (Cycle 12 HEAD vs Cycle 16 origin/dev). Took origin/dev since it represents the newer operational state — duty-review.md is a rolling report, so the most recent data wins. No functional code conflicts in this merge.
