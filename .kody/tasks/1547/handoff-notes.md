Task 1547: Merge conflict resolution.

One conflicted file: `.kody/last-run.jsonl` (UU status). This is a JSONL session log file.

Resolution: Took HEAD version (stage 2 from git index). Both HEAD and origin/dev stages were clean — conflict markers were only in the working-copy file due to a previous failed merge attempt.

Files changed: `.kody/last-run.jsonl` — conflict resolved, no code changes.
