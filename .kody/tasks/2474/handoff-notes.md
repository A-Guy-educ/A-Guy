Resolved the single conflicted file `.kody/reports/duty-review.md`. The conflict was asymmetric: the PR branch carried an older Cycle 7 snapshot while `origin/dev` had the newer Cycle 16 snapshot of the same auto-generated duty review report. Since this is a rolling job-generated report and the PR's actual work (admin dashboard widgets) is unrelated to the report, I accepted `origin/dev`'s Cycle 16 as the authoritative state. No conflict markers remain.
