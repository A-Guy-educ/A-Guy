Resolved a single asymmetric conflict in next.config.js where the admin route CSP img-src directive differed between HEAD and origin/dev. HEAD had `gravatar.com www.gravatar.com` (two specific domains) while origin/dev had `*.gravatar.com` (wildcard). The wildcard is the correct choice — it covers all gravatar subdomains and is consistent with how other domain wildcards are handled in the CSP. Removed all conflict markers; the merged file is clean with no remaining `<<<<<<<`, `=======`, or `>>>>>>>` markers.
