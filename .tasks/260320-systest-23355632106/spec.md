# Logger Utility Comment Addition

## Overview

Add a descriptive comment to the logger utility file explaining its purpose.

## Requirements

- Add the following comment to `src/infra/utils/logger/index.ts`:
  `// Logger utility for structured JSON logging with Pino`

## Acceptance Criteria

- [ ] Comment "// Logger utility for structured JSON logging with Pino" is present at the top of `src/infra/utils/logger/index.ts`
